# Dev setup only. Please don't turn this on in production. 
limit.maxIDLength: # Sets the maximum length allowed for workflow IDs, run IDs, activity IDs, etc.
  - value: 255
    constraints: {}
system.forceSearchAttributesCacheRefreshOnRead: # Forces Temporal to refresh the search attribute cache every time it is read. 
  - value: true 
    constraints: {}
